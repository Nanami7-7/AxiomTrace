# MSPM0G3507 串口控制命令参考

> 适用代码基线：`main` 分支，提交 `c3d5c55f4f5e`（2026-07-23）  
> 文档用途：UART0 调试终端、VOFA+/GUI 控制、DRV8870 工厂测试、编码器/ADC/IMU 诊断，以及 UART1/JDY-23 BLE 管理与透传。  
> 本文以当前实现为准；发送命令前请确认固件构建配置、电机悬空状态和电源限流设置。

---

## 1. 先看这里：紧急恢复命令

| 命令 | 作用 | 注意事项 |
|---|---|---|
| `StopAll` | 停止四路电机，并退出 FireWater 遥测循环 | 正常速度控制下的首选全停命令 |
| `abort` | 终止位置/角度控制，并停止全部电机 | 用于位置或转角运动中止 |
| `drvscope off` | 四路 PWM 恢复中性值、PB19 电机电源关闭、退出示波器会话 | 仅工厂测试构建有效 |
| `Stream=0` | 退出 FireWater 遥测循环 | **只停止数据流，不保证电机停止** |
| `STOP` | 退出 `enc`、`diag` 或 `adc` 等阻塞式诊断 | 必须使用大写；不是通用电机停止命令 |

> **安全原则**：需要停止电机时优先发送 `StopAll`；不要把 `Stream=0` 或诊断中的 `STOP` 当成电机急停。

---

## 2. 串口硬件与终端设置

### 2.1 UART0：上位机、调试菜单和 VOFA+ 控制

| 项目 | 配置 |
|---|---|
| MCU 串口 | UART0 |
| TX | PA10 |
| RX | PA11 |
| 波特率 | 115200 |
| 数据格式 | 8N1，无流控 |
| 主要用途 | 调试终端、菜单命令、VOFA+/GUI、FireWater 遥测、BLE 管理命令入口 |

终端发送规则：

- ASCII 文本命令；以 `CR`、`LF` 或 `CRLF` 结束。
- UART0 输入支持回显。
- 支持 Backspace `0x08` 和 Delete `0x7F`。
- 菜单行缓冲为 64 字节，实际命令应控制在 **63 个可见字符以内**。
- VOFA 控制命令不区分 ASCII 大小写；诊断命令和 `ble ...` 顶层命令建议严格按本文小写形式发送。
- 未识别命令通常静默，不保证返回统一错误信息。

### 2.2 UART1：JDY-23 BLE 模块

| 项目 | 配置 |
|---|---|
| MCU 串口 | UART1 |
| TX | PB6，接 JDY-23 RX |
| RX | PB7，接 JDY-23 TX |
| 波特率 | 9600 |
| 数据格式 | 8N1，无流控 |
| 主要用途 | JDY-23 AT 管理和 BLE 透明传输 |

UART1 本身不是默认的人机调试终端。UART0 上输入 `ble ...` 命令，由固件通过 UART1 管理 JDY-23。

---

## 3. 电机编号和硬件映射

| 协议编号 | 逻辑名称 | 电机接口 | 车轮/编码器映射 |
|---:|---|---|---|
| `0` | A | M1 | RB，右后轮 |
| `1` | B | M2 | RF，右前轮 |
| `2` | C | M3 | LF，左前轮 |
| `3` | D | M4 | LB，左后轮 |

常用选择命令示例：

```text
Motor=0
Status=0
Run=0
Stop=0
```

---

## 4. 命令路由和运行状态

正常菜单状态下，命令大致按以下优先级处理：

```text
UART0/UART1-BLE 行输入
  ├─ ble ...                JDY-23 管理与透传
  ├─ drvscope ...           DRV8870 工厂示波器会话
  ├─ encdiag / enc / diag   编码器诊断
  ├─ adc                    ADC 序列诊断
  ├─ zutptest/turndtest/... IMU/KF 测试
  └─ VOFA 控制协议          电机、PID、运动和遥测
```

执行 `Run`、`Run=n` 或 `Stream=1` 后，菜单进入 FireWater 遥测循环。在这个循环里：

- 仍可使用 VOFA 控制命令，例如 `Target=...`、`Kp=...`、`Stop`、`StopAll`、`Stream=0`。
- `ble ...`、`drvscope ...`、`encdiag`、`adc` 等顶层诊断命令不会被分发。
- 要执行顶层诊断，先发送 `Stream=0`；如果电机也需要停下，应发送 `StopAll`。

---

## 5. 查询与 FireWater 遥测命令

### 5.1 设备信息查询

| 命令 | 说明 | 典型响应 |
|---|---|---|
| `Info?` 或 `Info` | 查询固件、协议、板卡、电机驱动和串口信息 | `@INFO,fw=...,proto=...,board=...,driver=...,motors=4,baud=115200,telemetry=firewater` |
| `Config?` 或 `Config` | 查询编码器、减速比、轮径、DRV8870 死区、遥测周期等 | `@CONFIG,ppr=...,gear_num=...,gear_den=...,decode=...,cpr=...,wheel_mm=...,...` |
| `Status?` 或 `Status` | 查询当前选中电机 | `@STATUS,motor=...,enabled=...,power=...,rpm=...,...` |
| `Status=n` | 查询编号 `n=0..3`，并把该电机设为当前选中电机 | 同上 |

`@STATUS` 的主要字段：

- `motor`：电机编号。
- `enabled`：该电机的软件运行使能。
- `power`：PB19 电机总电源命令状态。
- `rpm` / `target`：实测/目标转速。
- `output`：最终有符号电机命令。
- `kp/ki/kd`：当前速度 PID 参数。
- `ff_en/ff_k/ff_b`：前馈状态及参数。
- `mode`：`speed`、`position` 或 `angle`。

### 5.2 遥测开关

| 命令 | 说明 |
|---|---|
| `Stream=1` 或 `Stream=on` | 开启 FireWater 遥测；不会主动启动电机 |
| `Stream=0` 或 `Stream=off` | 关闭 FireWater 遥测；不会主动停止电机 |

当前遥测周期为 30 ms，文本格式为：

```text
ch0,ch1,ch2,ch3,ch4,ch5,ch6,ch7,ch8,ch9,ch10\n
```

11 个通道定义：

| 通道 | 含义 |
|---:|---|
| 0 | A/M1 实测 RPM |
| 1 | B/M2 实测 RPM |
| 2 | C/M3 实测 RPM |
| 3 | D/M4 实测 RPM |
| 4 | A/M1 目标 RPM |
| 5 | B/M2 目标 RPM |
| 6 | C/M3 目标 RPM |
| 7 | D/M4 目标 RPM |
| 8 | 当前选中电机的前馈输出 |
| 9 | 当前选中电机的 PID 修正输出 |
| 10 | 当前选中电机的最终有符号驱动输出 |

---

## 6. 基础电机控制命令

### 6.1 选择电机和设置目标

| 命令 | 参数 | 说明 |
|---|---|---|
| `Motor=n` | `n=0..3` | 选择当前电机 |
| `Target=x` | 有限浮点数 | 设置当前电机目标转速；固件钳位到 `-800..+800 RPM` |
| `Menu` | 无 | 请求刷新菜单状态显示 |

示例：

```text
Motor=0
Target=120
Status
```

### 6.2 PID 参数

| 命令 | 参数范围 | 说明 |
|---|---:|---|
| `Kp=x` | `-100..+100` | 当前电机比例增益 |
| `Ki=x` | `-100..+100` | 当前电机积分增益 |
| `Kd=x` | `-100..+100` | 当前电机微分增益 |

- `NaN`、`Inf` 会被拒绝。
- 超出范围的有限值会被钳位到 `±100`。
- 参数极性或数量级设置错误可能导致振荡、过流或机械冲击；应从低增益开始调试。

### 6.3 启停

| 命令 | 说明 |
|---|---|
| `Run` | 启动当前选中电机；重置该电机 PID，并进入 FireWater 遥测循环 |
| `Run=n` | 选择并启动 `n=0..3`，随后进入遥测循环 |
| `Stop` | 停止当前选中电机，并退出遥测循环 |
| `Stop=n` | 选择并停止 `n=0..3`，并退出遥测循环 |
| `StopAll` | 停止四路电机，并退出遥测循环 |

推荐的单电机闭环测试步骤：

```text
StopAll
Motor=0
Kp=0.8
Ki=0.3
Kd=0
Target=60
Run
```

运行中修改目标：

```text
Target=100
Target=-60
StopAll
```

> 首次测试应悬空车轮、使用限流电源，并从较低目标转速开始。

---

## 7. 前馈参数和辨识命令

### 7.1 手动前馈参数

| 命令 | 参数 | 说明 |
|---|---|---|
| `FFk=x` | 有限浮点数 | 线性前馈斜率 |
| `FFb=x` | 有限浮点数 | 线性前馈截距 |
| `FFe=0` | 0 | 禁用前馈 |
| `FFe=x` | 非 0 | 启用前馈；固件提示重新启停电机后应用 |
| `FFKp=x` | `-100..+100` | 前馈模式下 PID 修正比例增益 |
| `FFKi=x` | `-100..+100` | 前馈模式下 PID 修正积分增益 |
| `FFKd=x` | `-100..+100` | 前馈模式下 PID 修正微分增益 |

### 7.2 模型辨识与自动整定（高风险）

| 命令 | 当前实现 | 风险说明 |
|---|---|---|
| `Step` | 使用默认电机命令幅值 `300` 做阶跃辨识 | 会实际驱动当前电机 |
| `Step=x` | 当整数化后的 `x` 满足 `1..500` 时使用该幅值；否则回退默认 `300` | 负值当前不会形成反向阶跃，而是回退默认值 |
| `Auto` | 先以默认幅值 `300` 辨识，再以默认闭环带宽 `5 Hz` 计算并应用 PI，同时更新 `FFk` | 会修改当前 PID/前馈参数 |
| `Auto=x` | 当 `x>0.5` 时作为期望闭环带宽 Hz；代码当前未设置显式上限 | 过高带宽可能产生严重振荡或过流 |
| `Sweep` | 执行约 40 秒的前馈扫描，拟合并自动应用 `FFk/FFb`，同时使能前馈 | 长时间自动驱动，机械和热风险最高 |

执行这些命令前必须：

1. 只连接待测电机，或确认其他电机均已停止。
2. 悬空车轮并固定车体。
3. 使用具有限流能力的实验室电源。
4. 准备随时发送 `StopAll` 或使用硬件电源开关。
5. 记录原 PID/前馈参数，以便人工恢复。

> 当前头文件旧注释中出现过 `AutoBW=5`，但解析器实际支持的是 `Auto` / `Auto=x`，不要发送 `AutoBW=...`。

---

## 8. 位置和角度运动命令

| 命令 | 参数 | 说明 |
|---|---|---|
| `mode=speed` | 无 | 切换到速度控制模式 |
| `mode=position` | 无 | 切换到位置控制模式 |
| `mode=angle` | 无 | 切换到转角控制模式 |
| `pos=<pulses>,<cruiseRPM>` | 位移脉冲、巡航 RPM | 以四路编码器平均位置执行位移控制；会自动切到 position 模式 |
| `angle=<degree>,<cruiseRPM>` | 目标角度、巡航 RPM | 基于 IMU yaw 执行转角控制；会自动切到 angle 模式 |
| `abort` | 无 | 紧急终止位置/角度控制并停止全部电机 |

参数规则：

- 两个参数都必须是有限数值。
- `cruiseRPM` 必须满足 `0 < cruiseRPM <= 300`。
- `pulses` 和 `degree` 当前只检查是否为有限数值，没有额外绝对值上限；用户必须自行限制合理范围。
- `pos` 使用四路编码器平均值。如果有编码器未接、方向错误或存在噪声，位置控制结果可能失真。
- `angle` 依赖 IMU 初始化、陀螺零偏和 yaw 数据质量。

示例：

```text
StopAll
pos=1040,80
```

```text
StopAll
angle=90,60
```

任何异常立即发送：

```text
abort
```

---

## 9. DRV8870 示波器/直接 PWM 命令

### 9.1 构建门禁

这些命令只有在以下宏启用时才真正操作硬件：

```c
#define PRJ_DRV8870_FACTORY_TEST_ENABLE (1U)
```

当前 `project_config.h` 默认值为 `0U`。生产构建中输入命令只会提示功能被禁用，不会改变 PB19。

### 9.2 命令表

| 命令 | 说明 |
|---|---|
| `drvscope start` | 四路 PWM 先设为 50.0% 中性，再开启 PB19，并锁定持久示波器会话 |
| `drvscope A <duty>` | 设置 A/M1 的直接 PWM 占空比 |
| `drvscope B <duty>` | 设置 B/M2 的直接 PWM 占空比 |
| `drvscope C <duty>` | 设置 C/M3 的直接 PWM 占空比 |
| `drvscope D <duty>` | 设置 D/M4 的直接 PWM 占空比 |
| `drvscope all <duty>` | 四路设置相同占空比 |
| `drvscope status` | 输出会话、PB19、PWM 时钟/周期/频率、各通道比较值和占空比 |
| `drvscope off` 或 `drvscope stop` | 恢复中性值、关闭 PB19、释放会话 |

占空比规则：

- 范围 `0.0%..100.0%`。
- 支持整数或最多一位小数，如 `20`、`20.5`、`100.0`。
- 不接受负数、两位小数或 `100.1`。

例：

```text
drvscope start
drvscope status
drvscope A 80
drvscope B 20.5
drvscope all 50
drvscope off
```

DRV8870 当前控制约定：

```text
占空比 < 40%      反转区
40% <= 占空比 <= 55%  软件死区/中性区
占空比 > 55%      正转区
50%                中性初值
```

> `drvscope` 绕过正常速度闭环。`drvscope start` 后 PB19 会持续保持 ON，直到显式执行 `drvscope off/stop`、硬件故障退出或 MCU 复位。不要把 50% 理解为普通单端 PWM 的“半速”。

---

## 10. 编码器和 ADC 诊断命令

### 10.1 编码器命令

| 命令 | 说明 | 退出方式 |
|---|---|---|
| `encdiag` | 输出一次只读硬件映射和编码器快照；不发 PWM、不清编码器 | 自动返回 |
| `encdiag cap` | 默认对 ALL 采集 1000 ms 的 CC0/CC1 捕获诊断；仅工厂测试构建有效 | 自动返回 |
| `encdiag cap A [ms]` | 诊断 A/M1 编码器（RB） | 自动返回 |
| `encdiag cap B [ms]` | 诊断 B/M2 编码器（RF） | 自动返回 |
| `encdiag cap C [ms]` | 诊断 C/M3 编码器（LF） | 自动返回 |
| `encdiag cap D [ms]` | 诊断 D/M4 编码器（LB） | 自动返回 |
| `encdiag cap ALL [ms]` | 同时诊断四路 | 自动返回 |
| `enc` | 每 50 ms 输出四路编码器累计计数 CSV | 输入大写 `STOP` |
| `diag` | **使能并驱动当前选中电机**，以当前目标转速执行约 5 秒编码器/控制诊断 | 输入大写 `STOP`，或 50 组采样后自动停止电机 |

`encdiag cap` 的命令字母统一按电机接口解释：`A=M1`、`B=M2`、`C=M3`、`D=M4`。底层编码器映射仍分别为 `RB/RF/LF/LB`。时间窗口最终钳位到 `100..10000 ms`。示例：

```text
encdiag cap ALL 3000
encdiag cap B 8000
```

注意：

- 捕获诊断主要用于检查 CC0/CC1 中断、捕获值、GPIO 电平、LOAD 溢出和噪声。
- 未接编码器的输入若悬空，可能持续产生伪边沿；这不是车轮真实运动。
- `encdiag cap ...` 在 `PRJ_DRV8870_FACTORY_TEST_ENABLE=0` 的生产构建中会直接提示禁用，不执行采集。`encdiag` 一次性只读快照不受此门禁影响。
- **`diag` 不是只读命令**：它会重置当前电机 PID、置运行使能，并按当前 `Target` 驱动约 5 秒；必须先悬空车轮并确认选中的电机和目标值。

### 10.2 ADC 序列诊断

```text
adc
```

采样通道：

| ADC 序列 | 信号 | 引脚 |
|---|---|---|
| MEM0 | M1 电流采样 | PA15 |
| MEM1 | M2 电流采样 | PA16 |
| MEM2 | M3 电流采样 | PA17 |
| MEM3 | M4 电流采样 | PA22 |
| MEM4 | 外部电池电压采样 | PB18 |

行为：

- 初始化 ADC 后最多采样 100 组。
- 每组成功采样后延时 200 ms。
- 输出 `raw/mV`；换算使用 `raw × 3300 / 4096`。
- 序列转换失败时打印 `TIMEOUT`。
- 可在运行中输入大写 `STOP` 提前退出。
- 此诊断输出的是 ADC 引脚电压；电机电流和电池实际电压还需要结合外部分压、运放增益、采样电阻和校准参数换算。

---

## 11. IMU、转向和 KF 测试命令

### 11.1 IMU 状态与连续打印命令

当前工程提供独立、受控的 IMU 控制台命令。上电后连续输出默认关闭，IMU 的 100 Hz 采集和滤波不受影响。

| 命令 | 参数/范围 | 说明 |
|---|---|---|
| `imu` | 无 | 等价于 `imu status` |
| `imu status` | 无 | 打印一次可读的姿态、加速度、角速度、温度、样本年龄和发送统计 |
| `imu help` | 无 | 显示 IMU 命令帮助 |
| `imu format compact` | 无 | 设置三通道 CSV：`roll,pitch,yaw` |
| `imu format full` | 无 | 设置 17 通道完整 CSV |
| `imu rate <ms>` | `20..1000` ms | 设置连续输出周期；默认 100 ms |
| `imu stream start` | 无 | 开始连续 CSV 输出，不启动或停止电机 |
| `imu stream stop` | 无 | 停止连续 CSV 输出并恢复文本菜单刷新 |

一次状态查询示例：

```text
imu status
```

典型输出：

```text
IMU console status:
  stream : OFF
  format : compact
  period : 100 ms
  tx     : frames=0 busy-drops=0
  sample : age=3 ms timestamp=123456 ms
  attitude(deg): R=-1.300 P=-2.200 Y=0.000
  accel(g)     : X=0.0012 Y=-0.0191 Z=0.9987
  gyro(dps)    : X=0.021 Y=-0.013 Z=0.008
  temp         : 25.4 C
```

紧凑三通道绘图：

```text
imu format compact
imu rate 100
imu stream start
```

输出列：

```text
roll,pitch,yaw
```

完整 17 通道绘图：

```text
imu format full
imu rate 100
imu stream start
```

完整输出列顺序保持与原 IMU DMA 遥测实现一致：

```text
ax, ay, az, gx, gy, gz, pitch, roll, yaw,
kf_p00_x, kf_p00_y, kf_p00_z, gyro_mag,
acc_norm_err, kf_p11_x, temperature, kf_bias_z
```

配置项位于 `Config/project_config.h`：

```c
#define PRJ_IMU_CONSOLE_ENABLE              (1U)
#define PRJ_IMU_CONSOLE_DEFAULT_PERIOD_MS   (100U)
#define PRJ_IMU_CONSOLE_MIN_PERIOD_MS       (20U)
#define PRJ_IMU_CONSOLE_MAX_PERIOD_MS       (1000U)
```

UART0 仲裁规则：

- IMU 连续输出与 11 通道电机 FireWater 遥测互斥。
- 在 FireWater 循环中发送任意 `imu ...` 命令，会先退出 FireWater 循环。
- IMU 连续输出期间发送任意非 IMU 命令，会先自动停止 IMU 流，再执行该命令。
- UART0 命令输入支持 CR、LF 和 CRLF；CRLF 的第二个空终止符会被忽略，不会误停止刚启动的 IMU 流。
- UART0 忙时 IMU 任务直接丢弃本次遥测帧，不阻塞 100 Hz IMU 采集；`imu status` 的 `busy-drops` 可用于观察竞争情况。
- `imu stream start` 只改变打印状态，不改变 PWM、电机使能、目标转速或控制模式。

正常菜单状态刷新仍会保留原来的低频摘要：

```text
IMU: R=-1.3 P=-2.2 Y=0.0 H=0.0 V=0.000
```

其中 `R/P/Y` 为姿态角，`H` 为互补导航模块 heading，`V` 为互补导航模块 X 向速度。
### 11.2 IMU/KF 测试命令

| 命令 | 默认值/范围 | 说明 |
|---|---|---|
| `zutptest` | 默认 60 秒 | 静止 ZUPT/yaw 漂移测试 |
| `zutptest N` | `N=1..86400` 秒；非法值回退 60 | 输出漂移、标准差、范围、bias 跟踪、故障和 verdict |
| `turndtest` | 默认 90°、60 秒超时 | 启动动态转向精度测试 |
| `turndtest ANGLE [TIMEOUT]` | 超时 `1..86400` 秒；角度解析为 0 时回退 90° | 记录转动和稳定阶段统计 |
| `turnend` | 无 | 人工确认当前转向动作已经结束 |
| `kftune` | 默认每组 10 秒 | 扫描 9 组 KF 参数 |
| `kftune N` | `N=1..600` 秒/组；非法值回退 10 | 执行 KF 参数扫描并输出评分 |

使用要求：

- `zutptest` 和静态段测试时保持设备静止。
- `turndtest` 若配合车辆实际运动，应悬空车轮或预留安全场地。
- 同一时间只应运行一个测试；已有测试活动时，新测试会被拒绝或不能按预期启动。
- `turnend` 只对正在运行的 turn test 有意义。

---

## 12. JDY-23 BLE 管理和透传命令

### 12.1 功能开关

```c
#define PRJ_BLE_MENU_ENABLE         (1U)
#define PRJ_BLE_MENU_CONSOLE_ENABLE (0U)
```

当前默认含义：

- `PRJ_BLE_MENU_ENABLE=1`：初始化 BLE 服务，并在 UART0 开启 `ble ...` 管理命令。
- `PRJ_BLE_MENU_CONSOLE_ENABLE=0`：手机发来的透明数据只进入 BLE 接收缓冲，**不会直接执行电机菜单命令**。
- 若把 `PRJ_BLE_MENU_CONSOLE_ENABLE` 改为 `1`，BLE 收到的 ASCII 行可复用 UART0 菜单解析器；此时手机可发送 `Motor=0\r\n`、`Target=60\r\n`、`Run\r\n` 等命令。

### 12.2 基础管理命令

| 命令 | 说明 |
|---|---|
| `ble` 或 `ble help` | 显示 BLE 命令帮助 |
| `ble status` | 显示 UART1 映射、初始化/探测状态、monitor 状态、RX 统计和溢出统计 |
| `ble probe` | 先用 `AT+VER\r\n` 探测，失败后回退到 `AT\r\n` |
| `ble inspect` | 顺序执行全部内置只读查询，并显示原始响应、解析值和前缀状态 |
| `ble rx` | 一次性读取并打印当前 BLE RX 缓冲 |
| `ble monitor on` | 每个菜单周期打印接收到的 BLE 数据 |
| `ble monitor off` | 关闭持续监视 |
| `ble flush` | 丢弃当前 BLE RX 缓冲 |

当 `PRJ_BLE_MENU_CONSOLE_ENABLE=1` 时，`ble rx`、`ble monitor on` 和 `ble flush` 不可用，因为 UART1 RX 已由 BLE 菜单行组装器消费。

### 12.3 封装的只读 AT 查询

```text
ble query <key>
```

支持的 key（名称匹配不区分 ASCII 大小写）：

| key | 发送给模块 | 已验证响应前缀 |
|---|---|---|
| `VER` | `AT+VER` | `+VER:` |
| `STAT` | `AT+STAT` | `+STAT:` |
| `MAC` | `AT+MAC` | `+MAC:` |
| `BAUD` | `AT+BAUD` | `+BAUD:` |
| `NAME` | `AT+NAME` | `+NAME:` |
| `STARTEN` | `AT+STARTEN` | `+STARTEN:` |
| `ADVIN` | `AT+ADVIN` | `+ADVIN:` |
| `HOSTEN` | `AT+HOSTEN` | `+HOSTEN:` |
| `IBUUID` | `AT+IBUUID` | `+IBUUID:` |
| `MAJOR` | `AT+MAJOR` | `+IBMAJOR:` |
| `MINOR` | `AT+MINOR` | `+IBMINOR:` |

示例：

```text
ble query VER
ble query NAME
ble inspect
```

### 12.4 有副作用的 AT 动作

```text
ble action <RST|DISC|SLEEP> CONFIRM
```

| key | AT 命令 | 可能副作用 |
|---|---|---|
| `RST` | `AT+RST` | 模块复位，回复可能来不及发送 |
| `DISC` | `AT+DISC` | 主动断开 BLE 连接 |
| `SLEEP` | `AT+SLEEP` | 模块进入睡眠，后续通信可能中断 |

`CONFIRM` 必须严格大写且不可省略。例如：

```text
ble action DISC CONFIRM
```

动作命令发生 `TIMEOUT` 不一定代表失败：模块可能在返回完整响应前已经复位、断连或睡眠。

### 12.5 任意 AT 命令

| 命令 | 行结束处理 | 用途 |
|---|---|---|
| `ble at <command>` | 自动追加 `CRLF` | 推荐的 JDY-23 AT 调试方式 |
| `ble atraw <command>` | 不追加行结束符 | 兼容特殊固件或手册要求 |

示例：

```text
ble at AT+VER
ble at AT+NAME
ble atraw AT
```

执行 AT 命令前建议先断开手机/对端 BLE 连接，避免模块处于透明传输状态而不接受 AT 指令。

### 12.6 BLE 透明传输

UART0 发送到手机：

```text
ble send hello
```

- 实际透明发送 5 字节 `hello`。
- 不自动追加 CR/LF。
- 若接收端需要文本行，应显式在应用协议中约定行结束符；当前 `ble send` 命令行本身不能直接嵌入二进制 CR/LF。

手机发送到 MCU，在默认宏配置下查看：

```text
ble monitor on
```

或单次读取：

```text
ble rx
```

已在硬件上验证过：

```text
ble send hello    -> 手机收到 hello
手机发送 world     -> 固件输出 [BLE RX 5] world
```

### 12.7 手机通过 BLE 控制菜单

若需要手机直接发送电机菜单命令，编译前设置：

```c
#define PRJ_BLE_MENU_ENABLE         (1U)
#define PRJ_BLE_MENU_CONSOLE_ENABLE (1U)
```

手机端必须每条命令追加 `CR`、`LF` 或 `CRLF`，例如：

```text
StopAll\r\n
Motor=0\r\n
Target=60\r\n
Run\r\n
```

风险：BLE 无线链路可能断开、延迟或丢包。用于电机控制时必须保留硬件关断方式，并建议后续增加通信看门狗、命令鉴权/解锁和失联自动停车机制。

---

## 13. 常用操作流程

### 13.1 查询设备并启动 A 电机

```text
Info?
Config?
Status=0
Kp=0.8
Ki=0.3
Kd=0
Target=60
Run=0
```

停止：

```text
StopAll
```

### 13.2 只看四路实时数据，不启动电机

```text
Stream=1
```

退出：

```text
Stream=0
```

### 13.3 检查 B 编码器捕获

```text
Stream=0
StopAll
encdiag cap B 3000
```

### 13.4 查看 ADC 五通道

```text
StopAll
adc
```

提前退出：

```text
STOP
```

### 13.5 JDY-23 联机和透传验证

```text
ble status
ble probe
ble query VER
ble query MAC
ble send hello
ble monitor on
```

测试结束：

```text
ble monitor off
```

---

## 14. 已知限制和实现注意事项

1. **`bench` 未接入命令分发**  
   启动帮助会显示 `Type 'bench' to run MATHACL benchmark.`，但当前源码没有对应解析分支，发送后不会执行。

2. **`mathdiag` 未接入命令分发**  
   启动帮助会显示 `Type 'mathdiag' to run MATHACL hardware diagnostic.`，但当前源码没有对应解析分支，发送后不会执行。

3. **未识别命令通常静默**  
   当前顶层菜单没有统一的 `Unknown command` 响应。自动化上位机应通过查询响应、ACK 或超时自行判断。

4. **遥测循环只解析 VOFA 命令**  
   进入 `Run`/`Stream=1` 后，先退出遥测再执行 BLE、ADC、编码器等顶层命令。

5. **生产构建禁用 DRVScope**  
   默认 `PRJ_DRV8870_FACTORY_TEST_ENABLE=0`。即使启动菜单显示 `drvscope`，生产构建也不会执行直接 PWM 操作。

6. **BLE 默认不是无线菜单控制台**  
   默认 `PRJ_BLE_MENU_CONSOLE_ENABLE=0`，手机数据只能被接收/显示，不能直接控制电机。

7. **阻塞诊断的退出词大小写敏感**  
   `enc`、`diag`、`adc` 内部要求大写 `STOP`；这与 VOFA 的 `Stop` 电机命令不是同一个处理路径。

8. **命令参数没有统一持久化**  
   PID、前馈等运行时设置通常保存在 RAM 中，复位后恢复编译期默认配置；如需持久化，应另行设计 Flash 参数存储和版本校验。

---

## 15. 命令速查表

```text
[紧急/恢复]
StopAll
abort
drvscope off
Stream=0
STOP                      # 仅退出 enc/diag/adc 阻塞诊断

[查询/遥测]
Info? | Info
Config? | Config
Status? | Status
Status=0..3
Stream=1 | Stream=on
Stream=0 | Stream=off

[电机/PID]
Motor=0..3
Target=<RPM>
Kp=<value>
Ki=<value>
Kd=<value>
Run | Run=0..3
Stop | Stop=0..3
StopAll
Menu

[前馈/辨识]
FFk=<value>
FFb=<value>
FFe=<0|nonzero>
FFKp=<value>
FFKi=<value>
FFKd=<value>
Step | Step=<1..500>
Auto | Auto=<bandwidth_Hz>
Sweep

[位置/角度]
mode=speed
mode=position
mode=angle
pos=<pulses>,<cruiseRPM>
angle=<degree>,<cruiseRPM>
abort

[DRV8870 工厂示波器]
drvscope start
drvscope A <0.0..100.0>
drvscope B <0.0..100.0>
drvscope C <0.0..100.0>
drvscope D <0.0..100.0>
drvscope all <0.0..100.0>
drvscope status
drvscope off | drvscope stop

[编码器/ADC]
encdiag
encdiag cap [A|B|C|D|ALL] [100..10000_ms]
enc
diag
adc

[IMU 状态/连续输出]
imu | imu status | imu help
imu format <compact|full>
imu rate <20..1000_ms>
imu stream <start|stop>

[IMU/KF]
zutptest [1..86400_s]
turndtest [ANGLE] [1..86400_s]
turnend
kftune [1..600_s_per_set]

[JDY-23 BLE]
ble | ble help
ble status
ble probe
ble query <VER|STAT|MAC|BAUD|NAME|STARTEN|ADVIN|HOSTEN|IBUUID|MAJOR|MINOR>
ble inspect
ble action <RST|DISC|SLEEP> CONFIRM
ble at <command>
ble atraw <command>
ble send <text>
ble rx
ble monitor on | ble monitor off
ble flush
```

---

## 16. 源代码定位

| 内容 | 文件 |
|---|---|
| UART0 菜单、诊断和 BLE 命令分发 | `Application/Tasks/task_menu.c` |
| VOFA 命令解析、执行、查询响应 | `Application/app_vofa.c`、`Application/app_vofa.h` |
| 编码器、ADC、DRVScope 调试实现 | `Application/app_debug.c`、`Application/app_debug.h` |
| IMU 状态与连续遥测 | `Application/app_imu_console.c`、`Application/app_imu_console.h`、`Application/Tasks/task_imu.c` |
| IMU/KF 测试 | `Application/Tests/app_test_runner.c`、`Application/Tests/app_test_runner.h` |
| BLE 应用服务 | `Application/app_ble_service.c`、`Application/app_ble_service.h` |
| JDY-23 驱动和命令元数据 | `BSP/Communication/jdy23.c`、`BSP/Communication/jdy23.h` |
| 项目构建开关和参数 | `Config/project_config.h` |
| 基础上位机协议 | `Docs/通信协议_v1.md` |



